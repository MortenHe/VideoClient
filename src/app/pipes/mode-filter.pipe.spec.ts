import { ModeFilterPipe } from './mode-filter.pipe';

describe('ModeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ModeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
